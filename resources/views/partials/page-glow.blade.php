{{-- Ambient background accents adapted from the welcome hero. Include as
     the first child of a `relative` page root; the tint is a radial
     gradient (styles in app.css) so it fades out without hard edges and
     never intercepts the pointer. --}}
<div aria-hidden="true" class="page-glow pointer-events-none absolute inset-0 print:hidden"></div>
