{{-- Ambient background accents adapted from the welcome hero. Include as
     the first child of a `relative` page root; the single wrapper keeps
     stagger-children cascades intact and never intercepts the pointer. --}}
<div aria-hidden="true" class="pointer-events-none absolute inset-0 overflow-hidden print:hidden">
    <div class="glow -top-24 start-[-8rem] size-80 bg-teal-500/10 dark:bg-teal-400/5"></div>
    <div class="glow top-64 end-[-10rem] size-96 bg-purple-500/8 dark:bg-purple-400/5"></div>
</div>
